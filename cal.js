//숫자열로 바꾸기

function plus() {
	let a = prompt("첫번째 숫자를 입력해주세요");
	let b = prompt("두번째 숫자를 입력해주세요");
	a = parseInt(a);
	b = parseInt(b);

	alert(a + b);
}
// plus(); //함수가 이름이 불려서 실행됨.함수는 이름으로 불러주자

function minus() {
	let a = prompt("첫번째 숫자를 입력해주세요");
	let b = prompt("두번째 숫자를 입력해주세요");
	a = parseInt(a);
	b = parseInt(b);
	alert(a - b);
}

function multi() {
	let a = prompt("첫번째 숫자를 입력해주세요");
	let b = prompt("두번째 숫자를 입력해주세요");
	a = parseInt(a);
	b = parseInt(b);

	alert(a * b);
}

function ddd() {
	let a = prompt("첫번째 숫자를 입력해주세요");
	let b = prompt("두번째 숫자를 입력해주세요");
	a = parseInt(a);
	b = parseInt(b);
	alert(a / b);
}
