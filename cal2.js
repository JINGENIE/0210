function puls() {
	let a = prompt("첫번째 숫자를 입력하세요");
	let b = prompt("두번째 숫자를 입력하세요");

	a = parseInt(a);
	b = parseInt(b);

	alert(a + b);
}

function minus() {
	let a = prompt("첫번째 숫자를 입력하세요");
	let b = prompt("두번째 숫자를 입력하세요");

	a = parseInt(a);
	b = parseInt(b);

	alert(a - b);
}

function double() {
	let a = prompt("첫번째 숫자를 입력하세요");
	let b = prompt("두번째 숫자를 입력하세요");

	a = parseInt(a);
	b = parseInt(b);

	alert(a * b);
}

function devide() {
	let a = prompt("첫번째 숫자를 입력하세요");
	let b = prompt("두번째 숫자를 입력하세요");

	a = parseInt(a);
	b = parseInt(b);

	alert(a / b);
}
